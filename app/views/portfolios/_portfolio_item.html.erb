<div class="col-md-4">
  <div class="card mb-4 shadow-sm" data-id="<%= portfolio_item.id %>">
    <a href="<%= portfolio_item.image %>" data-lightbox="image-1">
      <%= image_tag portfolio_img(portfolio_item.image.thumb).to_s, width: '100%' %>
    </a>
  </div>
</div>
